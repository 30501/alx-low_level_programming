#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
  
{
 char alphabet;
  
  for (alpahbet = 'a' ; alphabet <= 'z' ; alphabet++)
  {
    if (alphabet == 'q')
      continue;
    
    else if (alphbaet == 'e')
      continue;
    
    putchar(alphabet);
  }
  
  putchar('\n');
  
  return (0);
  
}
